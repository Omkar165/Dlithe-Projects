import React from "react";

const ForgotPassword = () => {
  return (
    <div className="min-h-screen bg-gray-100 flex flex-col items-center justify-center">
      <div className="bg-blue-500 w-full text-center py-5 shadow-md rounded-t-md">
        <h1 className="text-2xl text-white font-bold tracking-wide">WELCOME</h1>
      </div>

      <div className="bg-blue-400 mt-[-20px] rounded-3xl p-8 w-[90%] sm:w-[400px] shadow-lg">
        <h2 className="text-xl font-bold text-black text-center mb-6">Forgot Password</h2>

        <div className="mb-4">
          <label htmlFor="newPassword" className="block text-white font-medium mb-2">
            New Password
          </label>
          <input
            type="password"
            id="newPassword"
            placeholder="Enter new password"
            className="w-full px-4 py-2 rounded-full border border-gray-300 shadow-md focus:outline-none focus:ring-2 focus:ring-white"
          />
        </div>

        <div className="mb-6">
          <label htmlFor="confirmPassword" className="block text-white font-medium mb-2">
            Confirm Password
          </label>
          <input
            type="password"
            id="confirmPassword"
            placeholder="Re-enter new password"
            className="w-full px-4 py-2 rounded-full border border-gray-300 shadow-md focus:outline-none focus:ring-2 focus:ring-white"
          />
        </div>

        <button className="w-full bg-gradient-to-r from-cyan-400 to-blue-500 text-white font-semibold py-2 rounded-full hover:opacity-90 transition duration-200">
          Confirm
        </button>
      </div>
    </div>
  );
};

export default ForgotPassword;
